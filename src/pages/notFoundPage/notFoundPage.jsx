import React from 'react'
import {
    Box
} from '../../utilities/muiComponents.js';

export default function NotFoundPage() {
    return (
        <Box align='center' sx={{ my: 40 }}>404 The requested page was not found.</Box>
    );
}